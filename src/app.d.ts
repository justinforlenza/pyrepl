import 'unplugin-icons/types/svelte'

declare global {
  const PKG: {
    version: string
  }
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}
